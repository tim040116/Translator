package etec.framework.security.encryption.factory;

import etec.framework.security.encryption.utils.Base64Tool;

public class EncryptionFactory {
	
	public static final Base64Tool base64 = new Base64Tool();
	
}
