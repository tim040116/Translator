SKIP_LIST=SYS_DTTM,ALL,ATTRIBUTES,OPERATOR,LE,LIKE ANY,LIKE,HAVING,VARBINARY,TIMESTAMP,DISTINCT,VALUES,VARBYTE,NULL,AND,OR,NOT,FROM,SELECT,JOIN,ON,WHERE,IN,BY,EXISTS,BETWEEN,WHEN,THEN,ELSE,VARCHAR,CHAR,DECIMAL,${TXDATE1},AS,INSERT,OVER,USING
OVER_LIST=RANK,SUM,ROW_NUMBER
DATA_TYPE_LIST=VARCHAR,DATE,NUMBER,DECIMAL,INTEGER,BIGINT,CHAR,VARBINARY