SKIP_LIST=SYS_DTTM,SALES_STORE_RATE,SALES_PSD,BATCH_DTTM,ALL,ATTRIBUTES,OPERATOR,LE,LIKE ANY,LIKE,HAVING,L_DAY_ID,TIMESTAMP,DISTINCT,VALUES,VARBYTE,NULL,AND,OR,NOT,FROM,JOIN,ON,WHERE,IN,EXISTS,BETWEEN,WHEN,THEN,ELSE,VARCHAR,CHAR,DECIMAL,${TXDATE1},AS,INSERT,O,OVER,R,SELECT,USING
OVER_LIST=RANK,SUM,ROW_NUMBER
DATA_TYPE_LIST=VARCHAR,DATE,NUMBER,DECIMAL,INTEGER,BIGINT,CHAR