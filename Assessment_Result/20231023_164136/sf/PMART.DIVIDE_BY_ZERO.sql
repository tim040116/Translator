REPLACE FUNCTION PMART.DIVIDE_BY_ZERO(NUMBER1 DECIMAL(20,6), NUMBER2 DECIMAL(20,6))
RETURNS DECIMAL(20,6)
LANGUAGE SQL 
DETERMINISTIC 
CONTAINS SQL 
CALLED ON NULL INPUT
SQL SECURITY DEFINER
COLLATION INVOKER
INLINE TYPE 1 
RETURN
CASE 
    WHEN (NUMBER2 = 0 OR NUMBER2 IS NULL) THEN 0
ELSE  
    NUMBER1/NUMBER2
END;